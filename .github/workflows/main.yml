name: Shopify Theme Sync

on:
  push:
    branches:
      - main
      - store-dev
      - store-stage
    paths-ignore:
      - '**/*.json'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Deploying theme (JSON changes ignored)"
